
!!! 5
%html
  %head
    %meta(charset="utf-8")
    -#%meta(httpEquiv="X-UA-Compatible"), content="IE=edge,chrome=1")
    %title
      %%block(name="title")
      \- Qalisa
    %meta(name='csrf-param', content='csrf_token')
    %meta(name='csrf-token', content=csrf_token())
    %meta(name="viewport", content="width=device-width, initial-scale=1.0")

    - for url in webassets('css'):
      %link(rel="stylesheet", href=url)

    %%block(name="extra_header_tags")

  %body
    %header
      %nav.navbar.navbar-default.navbar-fixed-top.col-xs-12.col-lg-6.col-md-8.col-md-offset-3
        .container
          .navbar-header.page-scroll
            %button.navbar-toggle.collapsed{"aria-expanded" = "false", "data-target" = "#bs-example-navbar-collapse-1", "data-toggle" = "collapse", :type = "button"}
              %span.sr-only Toggle navigation
              %span.icon-bar
              %span.icon-bar
              %span.icon-bar
            %a.navbar-brand(href="#page-top")
              Initiation Watch
              %br
                %p Ezolwaluko
          .nav.collapse(id="#bs-example-navbar-collapse-1")
            %ul.nav.navbar-nav.navbar-center
              %li.dropdown
                %a.dropdown-toggle(data-toggle="dropdown", href ="#")
                  Search
                  %b.caret
              -#  %ul.dropdown-menu
              -#    %li
              -#      %a{:href => "#"} Is your surgeon registered?
              -#%li.dropdown
              -#  %a.dropdown-toggle(data-toggle = "dropdown",href ="#")
              -#    Get Help
              -#    %b.caret
              -#  %ul.dropdown-menu
              -#    %li
              -#      %a(href ="#") Is an initiate in trouble?
              -#%li.dropdown
              -#  %a.dropdown-toggle(data-toggle = "dropdown", href = "#")
              -#    Register
              -#    %b.caret
              -#  %ul.dropdown-menu
              -#    %li
              -#      %a(href = "#") As a traditional surgeon
              -#%li.dropdown
              -#  %a.dropdown-toggle(data-toggle = "dropdown", href = "#")
              -#    Find
              -#    %b.caret
              -#  %ul.dropdown-menu
              -#    %li
              -#      %a(href = "#") Find a Clinic
              -#%li.dropdown
              -#  %a.dropdown-toggle(data-toggle = "dropdown", href => "#")
              -#    Contact
              -#    %b.caret
              -#  %ul.dropdown-menu
              -#    %li
              -#      %a(href = "#") Contact Details




    %%block(name='pre_content')

    .container
      -# show flash messages
      - for category, message in (get_flashed_messages(with_categories=True) or []):
        - category = 'info' if category == 'message' else category
        - category = 'danger' if category == 'error' else category
        %p.alert(class_="alert-" + category)
          &= message

      ${next.body()}

    %%include(file='_footer.haml')

    - for url in webassets('js'):
      %script(src=url)

    %%block(name="extra_javascript")